arr = [29, 10, 14, 37, 13]
for i in range(len(arr)-1,-1,-1): #맨뒤에서 부터 탐색 (최댓값 비교 경우)
    max_idx = i 
    for j in range(i):
        if arr[max_idx] < arr[j]:
            max_idx = j
    arr[max_idx], arr[i] = arr[i], arr[max_idx]
print(arr)

    
